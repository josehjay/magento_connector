# Do NOT pin `gunicorn` or `pypika` here. Frappe pins these itself via a git
# URL in its own pyproject.toml, and `uv` requires a single, consistent URL
# per package across the whole resolution graph. Duplicating Frappe's pin here
# goes stale every time Frappe bumps it, breaking `bench update` with
# "conflicting URLs for package" errors. See scripts/check_frappe_pins.py.
requests>=2.28.0
